import React from 'react';

const RegButton = () => {
    return(
        <nav>
            <div>
                <a href="/">Register</a>
            </div>
        </nav>
    );
}

export default RegButton;
